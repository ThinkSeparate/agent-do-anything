# config.yaml
model:
  api_key: "sk-xxxxxxxxxxxxxxxxxxxx"  # 您的真实API密钥（注意：将敏感信息直接放在YAML中需确保文件安全）
  base_url: "https://api.deepseek.com/v1"
  name: "deepseek-reasoner"
  timeout: 120

agent:
  output_root: "agent_output"

# logging:
#   level: "INFO"
#   file: "app.log"

# database:
#   host: "localhost"
#   port: 5432
#   name: "myapp"